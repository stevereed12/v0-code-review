{
  "name": "@white80/routine",
  "version": "0.1.0",
  "private": true,
  "description": "White80 automated morning routine — runs the full pipeline, renders a PDF, and emails the brief.",
  "type": "commonjs",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "run-once": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@white80/engine": "*",
    "express": "^4.21.2",
    "node-cron": "^3.0.3",
    "pdf-lib": "^1.17.1",
    "playwright": "1.44.0",
    "resend": "^6.12.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22",
    "@types/node-cron": "^3.0.11",
    "typescript": "5.7.3"
  }
}
